# Побочные находки — отдельная очередь

По просьбе владельца от05.09.2026: новые проблемы, не относящиеся напрямую к текущему пакету мобильной загрузки/скролла, записывать сюда и исправлять отдельным следующим проходом. Не превращать сопутствующую находку в незапланированный рефакторинг.

Каждая запись содержит статус уверенности, файл/механизм, риск, воспроизведение и условия будущей приёмки. Подозрение не является подтверждённым багом. Основные visual/performance задачи остаются в `ASTRA-IMPLEMENTATION-PLAN-2026-09-05.md`.

## S001 — противоречие памятки о сессии админки

- Статус: подтверждённое противоречие документации, **код не исправлялся**.
- Где: `AGENTS.md`/`CLAUDE.md`, блок «Грабли» утверждает, что reload `/admin` разлогинивает и пароль хранится в памяти. Ранее в этих же инструкциях раздел сессии описывает12-часовую HttpOnly-cookie и сохранение входа после reload.
- Риск: следующий агент может принять старую инструкцию за актуальную и ошибочно переписать рабочую защиту/сессию. Это не доказательство нового production-багa авторизации.
- Будущая проверка: сверить фактические `Admin.tsx`, `/api/admin/auth`, middleware сессии и разрешённый тест reload. Уточнить только устаревшую документацию, синхронно в обоих файлах; структуру/безопасность не менять.

## S002 — возможное пересечение чека и CTA на мобильной Meta Apps

- Статус: визуальное подозрение, не исправлялось. В browser smoke390px верх декоративного чека INSTALL оказался рядом с правым краем второй CTA «Сценарии продвижения» и визуально пересекал её область. Desktop1440px такого соседства нет.
- Где: `MetaAppsHeroVisual`, связанная раскладка Hero/LeftContent и стили сцены. Screenshot осмотрен во встроенном Chromium; точное пересечение DOM и hit-testing ещё не измерены.
- Риск: визуальный наезд на кнопку либо перекрытие её части. Нельзя объявлять кнопку неработающей без проверки.
- Следующий проход: воспроизвести на320/390/453, измерить границы и доступность клика, сравнить исходный дизайн. Исправлять только подтверждённое пересечение, не менять сцену целиком.

## S003 — разные критерии mobile при первом render и в effect

- Статус: подтверждённое различие условий в коде, визуальный дефект на границе ещё не воспроизведён.
- Где: `src/app/components/ui/use-mobile.ts`: initial использует `matchMedia('(max-width: 767px)')`, effect/listener — `window.innerWidth < 768`. Дробная ширина при zoom может давать разные ответы, как уже наблюдалось у preload консультации около1023px.
- Риск: Hero Meta Apps переносит блок статистики в зависимости от этого hook; различие может вызвать дополнительную перестановку после первого кадра. Не заявлять, что это причина всей мобильной задержки.
- Следующий проход: проверить767/767.33/768px и resize, сопоставить media CSS. Это UI-примитив: не переписывать его целиком, минимальную коррекцию делать только после воспроизведения.

## Правило продолжения

Не дублировать сюда уже запланированные H4/H5/H6/H7 дефекты. Новым находкам присваивать S002, S003… и ссылаться на них из итогов этапа. Ни одна запись сама по себе не означает разрешение на внешние действия или изменение данных владельца.
