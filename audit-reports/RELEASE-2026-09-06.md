# Выпуск 06.09.2026 — исправления мобильной загрузки и прокрутки

## Третий пакет — итог перед запрошенной выкладкой 06.09

Этот раздел уточняет исторические статусы ниже. Пакет подготовлен к отправке в main; успешный production-деплой подтверждать отдельно по Cloudflare check нового commit.

- **Meta Apps ≤380px:** три чека сдвинуты вместе на72px вниз. Сохранены сами чеки, их анимации и расстояния, телефон, подставка и кнопки. На320/380px зазор INSTALL/CTA в измеренных кадрах10.89/13.67px; на320/380/390/453/1440px кнопка доступна во всех15 точках hit-test, горизонтальное переполнение0. Выше380px композиция не менялась.
- **Home /#contact и /#social:** прежний scrollIntoView попадал на высоты заглушек, затем предшествующие секции догружались и уводили цель под шапку. Новый deferredAnchor ждёт реальные секции до цели и шрифты, сверяет геометрию wrapper в двух кадрах и прокручивает один раз с navbar+8px. Reveal-transform дочернего блока не входит в расчёт. Смена только hash тоже повторно запускает координацию. Обычная главная без hash остаётся ленивой; секции после цели не загружаются принудительно.
- **Отмена старого перехода:** useScrollTo прекращает отложенный поиск при wheel/touch, новой цели и unmount; Home также при новом pointerdown. Ctrl+Home/End теперь считаются ручной прокруткой документа, но не внутри текстового поля. Ожидание имеет cleanup и предельный срок, без позднего принудительного прыжка.
- **Проверено локально:** начало contact320/390/453/1440px —88.77/88.65/88.98/88.40px при нижней границе navbar80.67px; overflow0. Contact→social без reload:88.40px. Ручная прокрутка вверх не возвращалась назад при повторном наблюдении. Это не тест настоящего iPhone/WebKit и не полный аудит всех переходов.
- **Проверки финального кода:** npm run check завершён exit0,27 generated routes, SEO14/14; regression49/49, отдельные anchor+scroll25/25. Первый финальный запуск оборвался после Vite build на Windows/libuv assertion; повторный полный запуск без изменения кода прошёл до конца. Новых runtime-зависимостей, миграций, изменений tracking/consent и качества изображений нет.

**Остатки:** H2 согласование начального HTML и React без визуальной подмены; H1 сопоставимые мобильные/desktop cold/warm замеры; H4 остальные Navbar/service/history scroll-сценарии (новая координация ограничена Home hash); H5 вложенный focus; H7 canvas/timebase; M1 полный заголовок политики в две строки. Весь сайт и каждая строка ещё не проверены. Детальный план — ASTRA-IMPLEMENTATION-PLAN-2026-09-05.md, побочные находки — SIDE-FINDINGS-2026-09-05.md.

## Фактический статус

Изменения опубликованы: commit `ac1c17f`, `origin/main`. GitHub check **Cloudflare Pages: completed / success**, deployment `e6cd8589-21fc-4239-bd8e-8266fe42ceac`. Production HTML и браузер отдают новый entry `/assets/index-rIbvl3bU.js`; локальная сборка имеет другой hash (`index-1ITOq20S.js`), поэтому успех не определялся совпадением локального имени.

Этот файл — итог после выкладки, он уточняет исторические пометки «не выложено» в предыдущих отчётах. Код после проверенной сборки не менялся. Миграции, секреты, настройки Cloudflare и согласие/Meta CAPI не изменялись.

## Что исправлено и почему

| Исправление | Причина дефекта | Область действия |
|---|---|---|
| Полоса чтения юридического документа закреплена у границы содержимого модалки | Наследовался80px отступ под navbar отдельной страницы плюс padding scrollport | Общий Modal/LegalDoc; privacy проверена в браузере |
| Полоса пересчитывается при изменении размеров документа/окна | Высота после раскрытия/resize могла измениться без актуализации progress | LegalDoc + ResizeObserver |
| Отложенное восстановление прокрутки уступает ручному скроллу | RAF/таймер1200ms могли вернуть пользователя назад после нового wheel/touch/key | Общий history restore; hash-выравнивание DeferredSection услуг |
| Кнопки, не меняющие страницу, больше не замораживают запись скролла на1.8сек | Навигационное ограничение включалось на любой клик по кнопке | Общая маршрутизация |
| Окна не затирают место чтения значением0 | Fixed body показывает window.scrollY=0, визуальная позиция находится в отрицательном body.top | Сохранение позиции, вложенные окна, pagehide |
| Предзагрузка JS/CSS четырёх прямых входов начинается из HTML | Юридические страницы и thank-you отсутствовали в карте route entries | privacy-policy, offer, cookie-policy, thank-you |
| Консультация заранее загружает реальный фон | Preload указывал на старые workspace-картинки, которыми сцена не пользуется | consult, оба responsive-фона |
| Нет зазора media между1023 и1024px | При дробной ширине ни max1023, ни min1024 не совпадали | Complementary media для preload консультации |
| Страницы услуг сохраняют экземпляр при parent rerender | Фабрика создавала новый тип React-компонента, вызывая teardown/remount всех дочерних блоков | Google Ads, Meta Ads, Meta Apps, consult |

Не менялись изображения/их качество, цвета, композиция, размеры, длительности и easing анимаций. Новых runtime-зависимостей нет. Аудитные документы и тесты не импортируются публичным приложением.

## Проверки

- Полный `npm run check` после последней правки кода — exit0,27 generated routes, SEO14/14.
- Scroll suite9/9: отмена wheel/touch/key/scrollbar, cleanup, поздняя высота, fixed-body position.
- Lifecycle6/6: четыре service-типа сохраняют DOM/черновик и один mount, холодные imports дожидаются route+hero, retry после ошибки работает. До патча5/6 падали:3 mount вместо1.
- Локально: Meta Ads, Google Ads, Meta Apps — осмотр screenshot390/1440px, overflow0. Консультация — responsive проверки320/390/453/768/1024/1440, fractional boundary, modal/back, горизонтальный scroll.
- Production HTTP:14 статических публичных адресов +thank-you +несуществующий URL +13 актуальных статей/кейсов из sitemap =29 адресов. Все существующие200; отсутствующий404+noindex; thank-you noindex. У16 статических/служебных ответов подтверждён новый entry; материалы проверены на HTTP-выдачу, это не визуальный аудит каждого материала.
- Production browser390px `/consult/`: entry `index-rIbvl3bU.js`, активна только предзагрузка desk-v, page overflow0. Cookie→privacy: inner scroll1658, line offset относительно scrollport0. Escape→Escape: Y844 до открытия и844 после закрытия.
- Реальный iPhone/WebKit и полная интерактивная матрица всех страниц ещё не проверены. Lighthouse before с timeout не годится для обещаний итогового ускорения; сопоставимых after-замеров пока нет.

## Осталось (не потерять при переключении модели)

1. **H2/H1:** убрать несогласованную смену SEO-карточки на настоящий первый экран, сохранив дизайн/SEO; затем сопоставимые cold/warm замеры mobile+desktop. Это главная незакрытая причина некрасивой начальной загрузки.
2. **H7:** время движения сцен и canvas resize: паузы/возобновление, частота экрана, сохранение фазы/частиц. Не выключать эффекты ради оценки.
3. **H4:** остальные конкурирующие anchor/scroll задачи, Home deferred, возврат из каждого материала.
4. **H5:** возврат фокуса из вложенной privacy-модалки в cookie сейчас попадает на «Аналитика», а не opener. Остальные документы и callers проверить отдельно.
5. **M1:** полный заголовок политики в2 строки без обрезки (прямая просьба владельца),44px close,320/390/453 и доступность.
6. **Покрытие:** весь маршрутный и построчный аудит ещё не завершён. Инвентарь367 файлов/120212 строк — не утверждение о прочтении всех строк. Админка отдельно после публичной мобильной части.
7. **Побочные:** S001 противоречие документации админ-сессии; S002 возможное пересечение декоративного чека с CTA Meta Apps на390px, пока визуальное подозрение. См. SIDE-FINDINGS-2026-09-05.md.

Продолжать на «Высоком». «Средний» — только изолированная M1, затем снова «Высокий». Не нужен новый плагин или библиотека без доказанной необходимости.

## Дополнение: второй выпуск и следующий локальный пакет

Последующее уточнение владельца: эффект чека под кнопкой сохранять, поправить только узкое соседство. Локально добавлены три изменения top в media≤380px (`meta-apps-hero.css`); чеки целиком сохранены и сдвинуты вместе на72px, телефон/подставка/CTA/motion без изменений. Приёмка320/380/390/453/1440 и полный check прошли; подробности в S002. Это не исправление H4: смещение Home-якоря после deferred-догрузки остаётся следующей задачей.

- Второй выпуск `1b3e611` опубликован: Cloudflare Pages completed/success, deployment `5c989bb5-ff9f-4091-9ba7-63824e7a12fd`. Общие preloadable/heroPreload убирают лишний Suspense-fallback уже подготовленных hero, сохраняя холодную загрузку и retry. Это не закрывает смену SEO-shell на React и не является измерением ускорения. Владелец подтвердил отправку и попросил продолжать поиск багов; повторная проверка production в следующем проходе не выполнялась.
- Следующий пакет **локальный, не опубликован**: `useScrollTo` отменяет отложенный поиск якоря при ручном скролле, новой немедленной цели и unmount. Home отменяет ожидающий RAF по ручному скроллу и учитывает фактическую высоту navbar +8px вместо scrollIntoView под шапку. Дизайн, качество ассетов, easing и tracking не менялись.
- Проверки: `npm run check` exit0, regression39/39 (новые anchor6/6), SEO14/14,27 generated routes. Entry `index-DBA5Kvc0.js`. Браузер production-preview: Home/#contact на390 и1440px — начало секции88.42/88.38px, низ navbar80.67px, overflow0. Ручной up сохранил5599.33/6811.33px при повторном чтении после задержки; на390 проверены down/left/right, scrollX0.
- **H4 не закрыта:** на320px после догрузки начало contact оказалось144.56px; на453px29.30px при navbar80.67px, то есть перекрытие остаётся. Однократный правильный offset не стабилизирует высоту предшествующих deferred-секций. Не объявлять прямые якоря полностью исправленными. Реальный WebKit не проверялся.
- S002 подтверждена как визуальное пересечение чека INSTALL и второй CTA на390px, но hit-test в измеренной зоне возвращает кнопку первым элементом. Доказательства и дальнейшая приёмка — в SIDE-FINDINGS.
