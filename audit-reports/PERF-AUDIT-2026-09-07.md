# Аудит скорости и визуальной шлифовки — 06–07.09.2026

Исходные данные: раздел «Скорость» (PSI по 27 URL, реальные визиты за 28 дней) и собственные прогоны Lighthouse 13.4.1 против production и локального preview. Коммиты в `main` (локально, без push): `2677e6a`, `0a467aa`, `af5a331`.

## Что было доказано измерением

| Причина | Доказательство | Статус |
|---|---|---|
| Обложки статей — PNG по 1,3–2 МБ на i.ibb.co; фото кейсов на R2 до 0,8 МБ без размеров | Lighthouse production, статья mobile: LCP 12,7 с, вес 2 204 KiB, картинка 1 856 KB; оболочка кейса отдавала 10 фото без `loading="lazy"` | ✅ исправлено |
| PlexusBackdrop: сотни `stroke()` на кадр | 9 605 мс scripting за один замер, «The page loaded too slowly to finish within the time limit» — так PSI и падал с ошибками | ✅ исправлено |
| `public/_headers` склеивал `Cache-Control` | Live: `public, max-age=0, must-revalidate, public, max-age=31536000, immutable` на `/assets/*` | ✅ исправлено |
| `motion` 45 КБ gzip на каждой странице | чанк `motion-*.js` 136 КБ raw в `modulepreload` любого маршрута | ✅ 32 КБ gzip |
| Серверный санитайзер HTML не работал | DOMPurify+linkedom: `isSupported` ложь, `<script onclick>` возвращались как есть и на сборке, и в Functions | ✅ исправлено, тесты |
| Быстрое пролистывание показывает пустую заглушку секции | чанк секции приезжал позже, чем палец проходил 720px | ✅ прогрев после первого жеста |
| Пиксели (GTM/GA4/Метрика с webvisor/Meta/TikTok) | остаточный TBT на десктопе контентных страниц | ⏸ решение владельца |

## Что сделано

1. **Картинки статей** — `scripts/optimize-article-images.js` (шаг `optimize:images` в `npm run build`, `sharp`): 27 внешних картинок → 106 WebP-вариантов 480/768/1200/1600, 23,5 МБ → 6 МБ на все варианты. Манифест `public/images/articles/manifest.json` → `src/app/data/articleImageManifest.generated.ts`; `utils/articleImages.ts` подставляет `srcset/sizes/width/height` в обложку, витрины, карусель, вставки в тексте; генератор переписывает `<img>` в статической оболочке и объявляет обложку `preload`. Неизвестный адрес показывается как есть.
2. **Кэш статики** — `! Cache-Control` перед своим значением для `/assets/*`, `/images/*`, `/fonts/*`; тест в `generated-seo.test.js`.
3. **PlexusBackdrop** — линии и точки группируются по квантованному цвету (прозрачность 1/40, смешение 1/8) и рисуются одним `stroke`/`fill` на группу через `Path2D`. Физика, число точек, цвета и скорость не менялись.
4. **motion** — `m.*` + `LazyMotion(domAnimation)` в `App.tsx`; `MotionConfig reducedMotion="user"` сохранён; контракт-тест запрещает `motion.*`.
5. **Санитайзеры** — сборка: `scripts/article-sanitizer.js` (DOMPurify поверх jsdom, падает при `isSupported=false`); Functions: `functions/_lib/sanitize.ts` — allowlist-обход дерева linkedom; браузер — прежний DOMPurify, но статья разбирается один раз (`sanitizeHtmlToBody`) вместо трёх.
6. **Прогрев секций** — `utils/sectionWarmup.ts`: после первого скролла/касания Home и лендинги услуг догружают чанки секций по порядку. Проверено headless: до жеста — только ближайшая секция, после — остальные.
7. Документация: `CLAUDE.md`/`AGENTS.md` (команды, картинки статей, санитайзеры, `_headers`, правило `m.*`, устаревшая заметка о разлогине админки).

## Замеры (Lighthouse 13.4.1, mobile simulate)

| Страница | До (production) | После (локальный preview, без CDN и Brotli) |
|---|---|---|
| Статья `/blog/analitika-…` | 69, LCP 12,7 с, TBT 180 мс, 2 204 KiB, таймаут | 78, LCP 4,4 с, TBT 140 мс, 365 KiB, без предупреждений |
| `/blog/` | PSI не ответил за 90 с | 78, 330 KiB, без предупреждений |
| Главная | PSI 58 | 74, LCP 5,0 с, TBT 180 мс, 784 KiB |

FCP 2,9 с на preview — цена HTTP/1.1 без сжатия и CDN; на production ожидается ниже. Настоящие цифры — только PSI после деплоя.

## Визуальный аудит

Headless Chrome (playwright-core, системный Chrome), 18 маршрутов × 320/390/768/1440, прокрутка вниз и обратно с наблюдателями `layout-shift` и `resource`:

- горизонтальных переполнений страницы — 0; элементов за краем экрана — 0; обрезанного текста кнопок — 0; битых картинок — 0;
- сдвигов вёрстки при прокрутке вниз и обратно (≥0,005) — 0; картинок, догружающихся при обратном скролле, — 0;
- мелкие тач-цели (<24px) — только текстовые ссылки подвала и подписи документов (высота строки 18–20px при шаге 27px) — не дефект по WCAG 2.5.8, дизайн не трогался;
- кадры: `output/visual-audit-2026-09-07b/` (по экрану), `output/visual-audit-2026-09-07/` (full-page — искажены блоками с 100vh, не использовать для выводов).

Реальный iPhone/WebKit не проверялся: эмуляция Chromium не заменяет Safari.

## Осталось и что решает владелец

- **Деплой**: три коммита ждут команды «деплой в main». После выкладки — прогнать PSI из админки заново; ждать 81/88 → выше на статьях, ошибки PSI на `/blog/` и статьях должны уйти.
- **INP 2,1 с (14 замеров)** без атрибуции: предложение — колонка `inp_target` в `web_vitals_daily` (миграция) и передача селектора цели из `webVitals.ts`, чтобы видеть, какой именно клик медленный.
- **Метрика с webvisor** — вероятный источник TBT 800–1200 мс на десктопе контентных страниц; отключение/отсрочка только по решению владельца.
- **Кейсы без своих обложек**: в CMS у обоих кейсов `image: /og-image.jpg`, а ключи `CASE_CATALOG_META` не совпадают с их slug'ами — витрина показывает общую брендовую картинку.
- **Прототип H2** (SSR первого экрана Meta Ads) лежит в рабочей копии незакоммиченным: `scripts/first-screen-entry.tsx`, правки `MetaAdsEditorialHero.tsx`, `meta-ads-editorial-hero.css`, `scripts/content-entry.tsx`.

## Дополнение после выкладки (07.09, коммиты 5340bc3 и 1121cf7)

Выложено в production: перечисленные выше пакеты, закрытие остатков плана 05.09 (M1 две строки заголовка политики, H5 возврат фокуса из документа, H7 единый timebase сцен, S003 один критерий «мобильный») и черновик SSR первого экрана удалён (из него оставлены настоящие ссылки на CTA Meta Ads).

Холодный первый визит на живом сайте (свежий профиль без кэша, Slow 4G 1,6 Мбит/с, RTT 150 мс, CPU ×4, Chrome 152; скрипт `scratchpad/cold-visit.mjs`):

| Страница | TTFB | FCP | LCP | Вес |
|---|---|---|---|---|
| Главная | 407 мс | 3,1 с | 4,6 с (небо сцены) | 757 KiB |
| Статья блога | 398 мс | 2,6 с | 2,6 с (текст) | 351 KiB |
| `/blog/` | 389 мс | 2,5 с | 2,5 с (текст) | 310 KiB |
| Meta Apps | 494 мс | 2,8 с | 6,2 с (текст в сцене) | 422 KiB |
| Консультация (до 1121cf7) | 503 мс | 3,2 с | 9,1 с (ноутбук сцены) | 1 209 KiB |

Причина у консультации: фон `desk-v.webp` 416 КБ качался 3,4 с, а предметы сцены обнаруживались только после чанка хиро на 7-й секунде. Исправлено в `1121cf7`: вариант 800×1200 для 2× экранов через `image-set`/`imagesrcset` (PSNR 37,4 дБ к оригиналу, уменьшенному браузером), preload ноутбука, блокнота и трёх начертаний Commissioner.

Раздел «Скорость» в админке: результаты PageSpeed кэшируются 12 часов, «Проверить все» после выкладки показывало старую версию. Теперь ключ кэша включает коммит выкладки (`CF_PAGES_COMMIT_SHA`); «Перепроверить» у строки всегда делал свежий прогон.

Lighthouse desktop на живой главной после выкладки: 96, TBT 30 мс, LCP 0,9 с. Предупреждение «страница не успела успокоиться» вызвано событием `engaged_view` Meta CAPI через 10 с после открытия — это намеренный трекинг, не менялся.
