# Построчный аудит — журнал находок

Читаю каждый файл целиком, по проходам из `ТРЕКЕР.md`.
Записываю сразу, включая то, что оказалось нормой — чтобы не перепроверять дважды.

Обозначения: 🔴 ломает • 🟠 портит данные или защиту • 🟡 хрупко • 🟢 норма

---

## Проход 1 — `functions/_lib`

### `auth.ts` (15 строк) — 🟢 норма, одна мелочь

`timingSafeEqual` выходит сразу при разной длине (строка 4) — это выдаёт длину
пароля по времени ответа. На практике неважно: длина пароля не секрет, а перебор
режется профилем `admin_login` (15 попыток за 15 минут). Не трогаю.

`verifyAdminPassword` без `ADMIN_PASSWORD` возвращает `false` — падает закрыто,
это правильно.

### `cache.ts` (20 строк) — 🟢 норма

`putCache`/`deleteCacheByUrl` без try/catch: если `caches.default.put` бросит
(например, у ответа `no-store`), исключение уйдёт вызывающему. Проверить у
вызывающих сторон, что put не зовут на некэшируемых ответах.

### `http.ts` (72 строки) — 🟡 одно замечание

- `readRequestText` (44–72) — потоковое чтение с лимитом, отмена и
  `releaseLock` в `finally`. Сделано аккуратно, ошибок нет.
- `getClientIp` (24–34): если `CF-Connecting-IP` отсутствует, возвращается
  **строка `'unknown'`**. Это значение уходит ключом в rate-limit — см. ниже.

### `meta-pii.ts` (35 строк) — 🟢 норма

Нормализация совпадает с клиентской (это важно для сопоставления Lead и
QualifiedLead в Meta, и в коде это прямо прокомментировано).
`hashOrValidatePrehashed` возвращает `valid: false` в смысле «пришло сырое
значение, я его захешировал», а не «отклонить». Имя поля сбивает с толку —
проверить, что вызывающие стороны читают его правильно.

### `rate-limit.ts` (81 строка) — 🟡 три замечания, все известные компромиссы

1. **Ключ `'unknown'`** (строка 48). Без `CF-Connecting-IP` все такие запросы
   попадают в **один общий счётчик**. Для профиля `lead` это 20 запросов за
   10 минут на всех сразу. На Cloudflare заголовок есть практически всегда,
   поэтому риск теоретический, но осадок есть.
2. **Гонка на счётчике** (56–77): read-modify-write через Cache API. Два
   одновременных запроса прочитают одно значение и запишут одно и то же —
   лимитер занижает счёт. Это ограничение способа, а не ошибка.
3. **Стык окон**: окна фиксированные, поэтому на границе можно уложить 2×
   лимита за короткий промежуток. Классика фиксированного окна.

Мелочь: `max-age` счётчика ставится на `windowSeconds` от «сейчас», хотя рядом
уже посчитан `secondsUntilWindowEnd`. На корректность не влияет (ключ содержит
номер окна), только мусорит в кэше.

### `url-sanitize.ts` (34 строки) — 🟠 одна настоящая дыра

Строки 19–21:

```ts
} catch {
  return rawUrl;
}
```

Если URL не парсится, функция возвращает **исходную строку без очистки**.
Смысл функции — не пустить email/телефон/токен из адреса в CRM и в логи
(это написано в её собственной шапке). На кривом URL она этого не делает.

Правильно: вернуть `undefined` либо вырезать чувствительное и из непарсимой
строки. Сейчас единственный путь, где PII может просочиться, — самый
неаккуратный вход.

### `migration-guard.ts` (77 строк) — 🟢 норма

Разбор ошибки SQLite, подстановка имени миграции из сгенерированной карты,
единый 503-ответ. Ошибок нет.

### `d1.ts` (263 строки) — 🟠 два серьёзных вопроса

**1. Дату публикации нельзя изменить — она заблокирована дважды.**

Строка 221:
```ts
const publishedAt = previous?.publishedAt || article.publishedAt || nowIso;
```
Строки 206–208:
```sql
published_at = COALESCE(articles.published_at, excluded.published_at)
```

И в JS, и в SQL старое значение имеет приоритет над новым. Значит для уже
существующей статьи **новая дата публикации из админки не сохранится**.
А календарь публикаций (`ArticleCalendar`) строится именно на точной
`publishedAt`. Требует проверки со стороны админки — отмечено, проверю
в проходе 2 на `api/admin/articles.ts`.

**2. Сохранение статей удаляет всё, чего нет во входящем списке.**

Строки 251–256: всё из `existingArticles`, чего нет в присланном наборе,
удаляется. Защита от полного стирания есть (`normalized.length > 0`), но
если клиент пришлёт **частичный** список, остальное будет удалено.
Проверить вызывающую сторону.

Мелочь: `updateSet.unshift('id = excluded.id')` (строка 210) — при конфликте по
slug перезаписывается первичный ключ. Если входящий id уже занят другой
строкой, весь `batch` упадёт.

Кэш колонок (32–43) живёт 5 минут: сразу после применения миграции воркер
до пяти минут будет считать, что колонки ещё нет.

### `tracking-signature.ts` (180 строк) — 🟢 норма, сделано грамотно

Проверил отдельно самое хрупкое место — SQL захвата nonce (62–70): четыре
плейсхолдера, четыре бинда, `ON CONFLICT ... WHERE expires_at < now`.
Поведение верное во всех трёх случаях: новый nonce — захват; живой повтор —
отказ; истёкший — повторный захват (и это безопасно, потому что старую метку
времени всё равно отбросит проверка на строке 112).

Разделение D1/KV тоже верное: в режиме `enforce` до неатомарного KV
исполнение не доходит ни по одной ветке.

### `jsonbin.ts` (380 строк) — 🔴 здесь лежит сломанная функция админки

#### 🔴 Дата публикации не сохраняется. Отложенная публикация не работает.

`applyFreshnessMetadata`, строка 222:

```ts
const publishedAt = previous?.publishedAt || previous?.updatedAt || article.publishedAt || nowIso;
```

Приоритет: старая дата → **время последней правки** → и только потом то, что
ввёл владелец. А `updatedAt` проставляется при каждом сохранении (строки 228 и
237). Значит после первого же сохранения у статьи всегда есть `updatedAt`,
и **введённая в админке дата публикации не может примениться никогда**.

Если у статьи не было `publishedAt`, но был `updatedAt`, датой публикации
станет время последней правки — то есть вместо введённой даты подставится
посторонняя.

Замок стоит трижды, в двух путях сохранения:

| Где | Что делает |
|---|---|
| `jsonbin.ts:222` (`applyFreshnessMetadata`) | общая функция, работает для **обоих** хранилищ |
| `d1.ts:221` | `previous?.publishedAt \|\| article.publishedAt` — второй замок |
| `d1.ts:207` (SQL) | `COALESCE(articles.published_at, excluded.published_at)` — третий |

**Что видит владелец.** В `Admin.tsx:1730` есть поле выбора даты и времени,
в `Admin.tsx:1718` — подпись «(запланирована на …)». То есть отложенная
публикация — заявленная функция. Владелец ставит дату, жмёт сохранить,
получает «успешно» — и дата тихо откатывается. Работает это только для
совершенно новой статьи, у которой раньше даты не было вообще.

Календарь публикаций (`ArticleCalendar`) строится на точной `publishedAt` —
он показывает даты первого сохранения, а не то, что задумал владелец.

**Как чинить:** поменять приоритет на «введённое значение важнее старого»
(`article.publishedAt || previous?.publishedAt || nowIso`) во всех трёх местах.
Пустая строка из редактора остаётся falsy, поэтому статья, у которой дату не
трогали, сохранит старую. Правку не вношу молча — это изменение поведения
данных, вынес в решения владельца.

#### 🟠 Запись в JSONBin рапортует об успехе, когда основной бин не записан

Строка 374:
```ts
if (!primaryOk && (!backupConfig || writeErrors.length > 1)) throw ...
```

Если основной бин не записался, а резервный записался, `writeErrors.length === 1`
и исключения не будет — функция вернёт успех. Но чтение (`fetchArticlesFromJsonBin`,
строка 332) сначала идёт в **основной** бин и берёт резервный только при ошибке
чтения. Основной читается, он просто устарел.

Итог: «сохранено» → перезагрузка → старый текст. Путь неактуален, пока включён
`USE_D1_ARTICLES`, но ошибка настоящая и ждёт своего часа.

#### 🟡 Slug из кириллического заголовка зависит от порядка статей

`toSafeSlug` (68–78) вырезает всё, кроме `a-z0-9-`. Кириллический заголовок
превращается в пустую строку, и берётся запасной вариант `article-${index+1}` —
то есть **номер по позиции в списке**. Стоит поменять порядок статей — и адрес
такой статьи изменится, а старый отдаст 404. Пока slug задают руками латиницей,
это не стреляет.

#### 🟡 Описание молча режется до 160 символов

`isValidArticlePayload` в `api/admin/articles.ts` пропускает описание до 2000
символов, а `normalizeArticles` (179–199) всегда перезаписывает его на
`stripHtml(...).slice(0, 160)`. Сервер принимает одно, сохраняет другое,
и не говорит об этом.

### `api/admin/articles.ts` (387 строк) — проверен вне очереди (разбирал вопросы из `d1.ts`)

- 🟢 Удаление лишних статей защищено: пустой набор при непустой базе
  отклоняется (строка 321), а `writeArticlesToD1` дополнительно требует
  `normalized.length > 0`. Клиент шлёт всегда полный список.
- 🟡 `id` статьи переприсваивается по позиции в массиве при каждом сохранении
  (строка 333, `id: index + 1`). Проверил: в базе нет ни одной таблицы,
  ссылающейся на статью по числовому id, поэтому сейчас безвредно. Но
  полагаться на этот id как на идентификатор нельзя.
- 🟡 Тело запроса разбирается `request.json()` **до** проверки пароля
  (строки 198 и 263) и без ограничения размера. В проекте для этого есть
  `readRequestText` с лимитом, здесь он не используется. Неаутентифицированный
  запрос может заставить воркер буферизовать большое тело (30 раз в минуту —
  столько пропускает лимитер).

### `admin-session.ts` (117 строк) — 🟢 норма

Проверил подпись и разбор cookie построчно. `readCookie` правильно склеивает
значение обратно, если в нём есть `=`. Срок проверяется до подписи (строка 89) —
это осознанный дешёвый отсев, подделать срок без ключа всё равно нельзя.
Ключ выводится из `ADMIN_PASSWORD`, поэтому смена пароля гасит все сессии.
`HttpOnly`, `Secure`, `SameSite=Strict` — всё на месте.

Особенность, не ошибка: у сессии нет своего идентификатора, поэтому «выйти на
одном устройстве» невозможно — только сменой пароля. В шапке файла это описано.

### `admin-totp.ts` (185 строк) — 🟢 норма, алгоритм разобран построчно

Криптографию проверял отдельно, потому что она написана вручную:

- `counterToBytes` — big-endian 8 байт, верно;
- динамическое усечение (строки 88–92): у SHA-1 дайджест 20 байт, `offset`
  максимум 15, значит `offset+3 = 18 < 20` — выхода за границу нет;
- `base32Encode`/`base32Decode` копят биты в 32-битной переменной без маски.
  Проверил: за раз извлекается максимум 12 младших бит, а `<<` в JS сохраняет
  младшие 32 — переполнение на результат не влияет. Верно.

Мелочь: комментарий обещает допуск ±30 секунд, фактически окно из трёх шагов —
до 90 секунд. Поведение нормальное, неточно описание.

### `admin-2fa.ts` (173 строки) — 🟠 резервный код можно использовать дважды

Для TOTP отметку сделали атомарной и это прямо прокомментировано —
`claimTotpStep` (строки 153–158):

```sql
UPDATE admin_2fa SET last_step = ? WHERE id = 1 AND last_step < ?
```

А для резервного кода — нет. `consumeBackupCode` (162–172):

```ts
const remaining = state.backupCodeHashes.filter((item) => item !== hash);
await env.DB.prepare('UPDATE admin_2fa SET backup_codes = ? WHERE id = 1')
  .bind(JSON.stringify(remaining)).run();
return true;
```

Прочитали → отфильтровали → записали, без условия. Два одновременных входа
с **одним и тем же** резервным кодом оба прочитают состояние, оба увидят код
в списке, оба запишут и **оба вернут `true`**. Код, который по описанию
одноразовый («повторно им уже не войти»), сработает дважды.

Плюс `state` приходит от вызывающей стороны и к моменту записи может устареть.

**Как чинить:** сделать как с TOTP — обновлять с условием на прежнее значение
и возвращать результат по числу изменённых строк:

```sql
UPDATE admin_2fa SET backup_codes = ? WHERE id = 1 AND backup_codes = ?
```

Практическая опасность невелика (нужно знать живой резервный код и попасть в
гонку), но заявленная гарантия сейчас не выполняется.

Отдельно, но это **описанный компромисс, а не ошибка**: при недоступной D1
`isAdmin2faEnabled` возвращает `false`, то есть вход идёт по одному паролю.
В шапке файла объяснено — обратный вариант запирал бы владельца снаружи.

### `form-guard-stats.ts` (105 строк) — 🟡 один день может соврать нулём

Файл сам ставит себе правило: «Выдавать усечённое число за точное нельзя».
Но есть путь, где именно это и происходит.

Строки 74–77 отмечают день неполным так:

```sql
UPDATE form_guard_daily SET throttled = 1
WHERE day = ? AND reason = ? AND throttled = 0
```

`UPDATE` меняет строку, только если она уже есть. Если суточный бюджет записей
исчерпан **раньше**, чем для этой причины появилась первая строка, обновлять
нечего — строка не создастся, и причина за этот день не будет видна вообще.
Не «не меньше N», а пусто.

Сценарий узкий (нужен поток отказов одного вида, начавшийся после исчерпания
бюджета другими), но он ровно про то, от чего файл защищается.

### `leads.ts` (684 строки) — 🟠 четыре находки

Файл написан аккуратно: сверил все связки «плейсхолдер → бинд» в динамическом
`UPDATE` (строки 233–350) — их 17, все совпадают. Транзакционная логика через
`db.batch` с проверкой `changes` и откатом захвата (412–419, 525–541) корректна.
Ниже то, что не сходится.

#### 🟠 Дедупликация заявок перестанет работать после 500 заявок

Строки 195–197:

```sql
SELECT id, email, phone, telegram_username, submissions_count
FROM leads WHERE deleted_at IS NULL ORDER BY id DESC LIMIT 500
```

Повторная заявка ищется **только среди последних 500**. Как только активных
заявок станет больше, старый контакт, написавший снова, создаст **дубль**
вместо «поднятия» существующей записи со счётчиком.

Это заявленное поведение из CLAUDE.md («повторная заявка поднимает существующую
со счётчиком, а не создаёт дубль»), и оно отключится само, тихо, по достижении
порога. Заодно: 500 строк вычитываются из базы **на каждую заявку**.

#### 🟠 Один и тот же человек не склеится, если написал телефон по-разному

`contactKeys` (90–96) нормализует телефон как «все цифры подряд»:

```ts
phone: String(phone || '').replace(/\D/g, '')
```

`+7 999 123-45-67` → `79991234567`, а `8 999 123-45-67` → `89991234567`.
Это **разные ключи**, дедупликация не сработает.

Показательно, что в поиске дублей внутри CRM (`leadDuplicates.ts`) телефон
сравнивают по **последним десяти цифрам** — то есть правильный подход в проекте
уже есть, но в приём заявок он не попал. Две части системы считают «тот же
человек» по-разному.

#### 🟡 Повторная заявка стирает оценку качества

Строка 290: `if (hasQuality) set.push("quality = ''")`, плюс сброс
`quality_revision`, `quality_action_id`, `quality_processing` (303–306),
и новый `event_id` (строка 239).

Значит: заявку пометили «целевой» → в Meta ушёл `QualifiedLead` → человек
написал повторно → оценка обнулилась. Пометив её целевой снова, владелец
отправит в Meta **второй** `QualifiedLead` уже с другим `event_id`, то есть
дедупликация его не поймает.

Возможно, так и задумано (повторное обращение — новое событие). Но это стоит
решить осознанно: сейчас поведение нигде не описано, а на счётчики Meta влияет.

#### 🟡 Соль обезличивания посетителей может оказаться публичной константой

Строка 661:
```ts
const salt = env.TRACKING_HMAC_SECRET || env.ADMIN_PASSWORD || 'ww-stats';
```

Если ни один секрет не задан, солью становится строка из открытого исходника.
Тогда «обезличенный отпечаток» перестаёт быть обезличенным: `IP + UA + дата`
перебираются, а соль известна. Оба секрета почти наверняка заданы, но
запасной вариант противоречит обещанию в комментарии двумя строками выше
(«Сырые IP/UA нигде не сохраняются» — да, но хеш становится обратимым).

Там же строка 659: в отпечаток идёт `x-forwarded-for`, если нет
`CF-Connecting-IP`. Этот заголовок подставляет клиент, то есть счётчик
уникальных можно накрутить. На Cloudflare до этого не доходит.

#### 🟡 Отправка в Telegram без таймаута

`sendLeadToTelegram` (605–629) вызывает `fetch` без `AbortSignal.timeout`,
хотя в остальном проекте таймауты стоят (`_lib/turnstile.ts`, `meta-capi.ts`).
Зависший запрос к Telegram будет держать фоновую задачу до лимита воркера.

### `lead-quality.ts` (334 строки) — 🟠 одна потеря сигнала в Meta

Файл сделан правильно и осторожно: гейт согласия (142), проверка квитанции
согласия с допуском на расхождение часов (85–115), отказ отправлять событие без
единого ключа сопоставления (196), `original_event_data` по документации Meta
(223–227), `action_source: 'system_generated'` — всё верно.

Отдельно проверил связку `consent_region`: она приходит из
`CookieConsentManager`, где по умолчанию `'UNKNOWN'`, а не пустая строка
(строка 263). Значит квитанция согласия из-за региона не «потеряется» —
это был мой первый подозреваемый, он оказался чист.

#### 🟠 В `QualifiedLead` и `UnqualifiedLead` не уходит страна, хотя она сохранена

`user_data` события (208–216) содержит `em`, `ph`, `fn`, `ln`, `fbp`, `fbc`,
`external_id`. Страны там нет — и `LeadQualityRow` (17–38) её даже не объявляет.

При этом миграция 0026 добавила в таблицу `leads` колонку `country`, и
`api/lead.ts` её заполняет из `CF-IPCountry`. То есть страна лежит в базе,
но в события качества не передаётся.

Для сравнения: обычное событие `Lead` страну шлёт (`country: hashedCountry`
в `api/lead.ts`). Получается, события качества у Meta имеют **меньше ключей
сопоставления**, чем исходное событие того же человека.

Это ровно тот случай, о котором вы говорили: данные есть, но в Meta не идут.
Добавляется просто — `country` в `LeadQualityRow`, в SELECT со стороны
`api/admin/leads.ts` и SHA-256 в `user_data.country`.

#### 🟢 Порядок отметок при успехе

Строки 317–318: сначала `markMetaEventSent`, потом `markOutboxSent`. Если
первая упадёт, событие останется в очереди и уйдёт повторно — но с тем же
`event_id`, поэтому Meta его отбросит как дубль. Безопасно.

### `meta-outbox.ts` (349 строк) — 🟡 одна недоработка счётчика попыток

Разобрал самое важное — захват записей (247–257). `UPDATE ... WHERE id IN
(SELECT ... LIMIT) RETURNING` в SQLite берёт блокировку записи, поэтому два
параллельных обработчика не заберут одну строку дважды. Возврат зависших
`sending` через 600 секунд тоже сделан правильно.

Проверил SQL уборки (340–345) с тремя разными сроками: события качества 180
дней, остальные подтверждённые 7 дней, «мёртвые письма» 30 дней — биндинги
стоят в правильном порядке.

`ON CONFLICT ... WHERE status='dead_letter'` (88) вместе с чтением
`meta.changes` даёт верный `inserted` во всех трёх случаях — проверил каждый.

#### 🟡 Счётчик попыток не сохраняется в момент захвата

Строка 268: `const attempts = Number(row.attempts || 0) + 1;` — число живёт
только в памяти. Захватывающий `UPDATE` (248) переводит запись в `sending`,
но **не увеличивает `attempts`** в базе.

Если isolate умрёт посреди отправки, запись через 600 секунд вернётся в оборот
с прежним `attempts`. Повторяющийся сбой такого рода будет крутить запись
бесконечно, не приближая её к `dead_letter`. Ограничителем остаётся только
возраст события — шесть суток.

Лечится одной правкой: `attempts = attempts + 1` прямо в захватывающем UPDATE.

#### 🟡 Уборка выполняется на каждом вызове, в том числе фоновом из `/api/pageview`

`DELETE` на строке 340 выполняется всегда и вне `try`. Проект в остальных
местах бережёт суточный лимит записей D1 бесплатного тарифа (см. шапку
`form-guard-stats.ts`), а здесь уборка идёт при каждом проходе очереди.

### `meta-diagnostics.ts` (275 строк) — 🟠 упирается в бесплатные лимиты Cloudflare

Логика правильная: колонки фильтруются по фактической схеме (228), кэш схемы
сбрасывается при ошибке (242), запись в KV и в D1 независимы, и ни одна не
может уронить отправку события. Рост таблицы в D1 ограничен — нашёл триггер
удержания на 90 дней в `migrations/0016`.

Проблема не в логике, а в объёме.

#### 🟠 Одна запись в KV на каждое событие

Строка 216: `kv.put(...)` вызывается для **каждого** события — включая каждый
`PageView`. У бесплатного тарифа Cloudflare KV лимит **1000 записей в сутки**.
Пятьсот просмотров страниц в день — это пятьсот записей только на диагностику,
плюс лиды и остальные события.

Когда лимит исчерпан, `kv.put` начнёт падать. Отправку событий это не сломает
(ошибка ловится на 218), но **диагностика перестанет отражать реальность**
в самый интересный момент — при всплеске трафика.

Контраст: в `form-guard-stats.ts` ради экономии записей выстроен целый суточный
бюджет с пометкой неполного дня. Здесь такой защиты нет.

#### 🟠 Одна запись в D1 на каждое событие

Строка 232 — `INSERT` на каждое событие, тем же порядком. Удержание 90 дней
защищает от бесконечного роста, но не от суточного лимита записей.

#### 🟡 Метка «уже отправлено» живёт 48 часов, а очередь заявок — трое суток

`markMetaEventSent` (270): `expirationTtl: 60 * 60 * 48`.
`MAX_AGE_MS` в `src/app/utils/leadRetryQueue.ts`: 3 суток.

Заявка, пролежавшая в офлайн-очереди дольше двух суток, придёт с тем же
`event_id`, но метка идемпотентности к тому моменту истечёт — и событие уйдёт
в Meta повторно. Собственное окно дедупликации Meta по `event_id` тоже около
48 часов, то есть такое событие может посчитаться дважды.

### `seo.ts` (383 строки) — 🟡 три мелких SEO-огреха, ошибок безопасности нет

Проверил экранирование во всех местах, где строки попадают в HTML и XML:
`escapeHtml` в мета-тегах и тексте, `xmlEscape` в sitemap и RSS, `safeJsonLd`
с экранированием `<`, `>`, `&` и U+2028/2029 внутри `<script type="ld+json">` —
всё на месте. Тело статьи вставляется после `sanitizeArticleHtml`. Дыр нет.

Канонизация тоже верная: статьи без завершающего слеша, разделы — со слешем
(`canonicalRoute`, 327–332), что совпадает с тем, что генерирует сборка.

#### 🟡 В sitemap у всех неартикульных страниц `lastmod` = сегодня, каждый день

Строка 337: `const lastmod = toIsoDate(articleDates[route]) || defaultLastmod;`

`articleDates` заполняется только для статей. Для `/`, `/faq`, `/meta-ads`
и остальных дата берётся из `defaultLastmod` — то есть **сегодняшняя**, и так
при каждой генерации. Поисковик видит сайт, где ежедневно меняются все
страницы разом, и со временем перестаёт доверять этому полю.

#### 🟡 В RSS `pubDate` — это дата правки, а не публикации

`resolveArticleDate` (81–87) ставит `updatedAt` первым. В RSS (строки 359–360)
это уходит в `<pubDate>`. Отредактировали старую статью — она всплывает у
подписчиков как новая.

#### 🟡 `renderArticleHtml` всегда ставит `robots: index, follow`

Строка 240 — жёстко, без учёта `status` и будущей `publishedAt`. Значит фильтр
черновиков обязан стоять **до** вызова этой функции. Проверяю это следующим
файлом (`article-page.ts`) — сам по себе рендерер защиты не даёт.

### `article-page.ts` (239 строк) — 🟠 две находки

Сначала снял подозрение из прошлого файла: черновики и будущие публикации
отсекаются на строке 179 (`filterVisibleArticles`) **до** рендера, так что
жёсткий `robots: index, follow` в `seo.ts` до них не доходит. Чисто.

Проверил и то, что описано в CLAUDE.md как больное место: страница статьи
действительно не подменяется оболочкой главной, canonical ставится через
HTMLRewriter, слеш уходит в 301. Всё так.

#### 🟠 Ответ зависит от User-Agent, но `Vary: User-Agent` не выставляется

Боту отдаётся `renderArticleHtml` (голая SEO-разметка), человеку — оболочка SPA.
Развилка — на `isBotRequest`, то есть **по заголовку User-Agent**.

При этом ответ для бота уходит с `Cache-Control: public, s-maxage=300,
stale-while-revalidate=900` (`CACHE_CONTROL.botArticle`) и **без** заголовка
`Vary: User-Agent`.

Любой общий кэш — CDN, корпоративный прокси — имеет полное право отдать
сохранённый ответ следующему запросу того же URL независимо от User-Agent.
То есть человек может получить голую SEO-страницу без стилей и без React,
а бот — оболочку SPA.

Это не теория, а прямое требование HTTP: содержимое зависит от заголовка
запроса → этот заголовок обязан быть в `Vary`. Лечится одной строкой.

#### 🟠 Заголовки копируются на уже переписанное тело

`getArticleShell`, строка 78:

```ts
const source = await sectionShell.text();
const neutral = source.replace(...);
return new Response(neutral, { status: 200, headers: sectionShell.headers });
```

Тело прочитано, изменено и отдано **со старыми заголовками**. Там остаются
`Content-Length` от исходного файла и, что хуже, возможный
`Content-Encoding: gzip` — при том что `.text()` уже распаковал содержимое.
Браузер, поверив заголовку, попытается распаковать обычный текст.

Ветка запасная (срабатывает, только когда статью опубликовали между сборками),
поэтому в обычной жизни не проявляется. Но именно поэтому и не заметят.

Рядом, для сравнения, сделано правильно: на строке 219 ответ собирается со
своим свежим набором заголовков.

#### 🟡 При сбое хранилища человек может получить 404 на существующую статью

Строка 183: при ошибке чтения человеку отдаётся
`next(.../${sectionPath}/${slug}/index.html)` — напрямую, без запасного
варианта с оболочкой раздела, который есть в основном пути (`getArticleShell`).
Если статью опубликовали между сборками, её каталога ещё нет → 404, хотя
статья существует.

### `articles.ts` (388 строк) — 🟠 одна операционная мина

Цепочка запасных источников проверена построчно и полностью совпадает с тем,
что описано в CLAUDE.md: в режиме D1 успешный ответ (в том числе **пустой**)
считается каноном, JSONBin в этом режиме не читается ни по одной ветке,
статический seed доступен только по явному флагу. Валидация снимка из R2
(`parseD1Snapshot`, 116–138) строгая — сверяется версия, источник, дата,
совпадение `total` с длиной массива, уникальность slug и id, и тип каждого поля.

Отдельно проверил подозрение на строковое сравнение дат в `isPublishedArticle`
(строка 39, `article.publishedAt > nowIso`): колонка `published_at` объявлена
как `TEXT` **без** `DEFAULT` (миграция 0001), значит в неё попадает только то,
что пишет приложение — всегда полный ISO с `T`. Сравнение корректно. Чисто.

#### 🟠 Смена пароля админки делает резервную копию статей нечитаемой

`getD1SnapshotKey` (144–153) выводит ключ объекта в R2 из `ADMIN_PASSWORD`:

```ts
const digest = await crypto.subtle.digest('SHA-256',
  new TextEncoder().encode(`${D1_SNAPSHOT_KEY_CONTEXT}${capabilitySecret}`));
return `${D1_SNAPSHOT_PREFIX}/${bytesToHex(digest)}.json`;
```

Сменили пароль → ключ другой → **последняя удачная копия статей перестаёт
читаться**. Новая появится только после следующего успешного чтения из D1.

Опасное сочетание: сменить пароль и в тот же момент потерять D1 — тогда
запасного источника нет вообще, и сайт отдаёт ошибку вместо статей.
Старый объект при этом остаётся в R2 навсегда, никто его не удаляет.

Для сессий админки (`admin-session.ts`) и для ссылок предпросмотра
(`page-lock-preview.ts`) вывод ключа из пароля — правильное решение: там смена
пароля **должна** всё обнулять. Здесь тот же приём даёт обратный эффект:
он обнуляет резервную копию, а это не то, чего от смены пароля ждут.

### `sanitize.ts` (83 строки) — 🟢 норма

Отдельно проверил самое подозрительное место — строку 74:

```ts
element.setAttribute('sandbox', 'allow-scripts allow-same-origin allow-presentation');
```

Сочетание `allow-scripts` + `allow-same-origin` обычно означает «песочница
отключена»: документ внутри может снять с себя ограничение. Но здесь `src`
уже прошёл `isSafeIframeSrc` — только https и только YouTube/Vimeo. Для
**чужого** источника `allow-same-origin` сохраняет его собственный origin,
а не даёт доступ к нашему. Это стандартное требование, чтобы плеер работал.
Дыры нет.

Хук `uponSanitizeElement` удаляет небезопасные iframe до разбора атрибутов —
порядок правильный. Списки тегов и атрибутов совпадают со вторым и третьим
санитайзером (сверял ранее).

🟡 Мелочь: в клиентском `src/app/utils/sanitizeHtml.ts` хуки вешаются на
**глобальный** экземпляр DOMPurify (`DOMPurify.addHook`), то есть действуют на
любой вызов `sanitize()` в приложении, а не только на статьи. Сейчас это
безвредно (других вызовов с другим смыслом нет), но это скрытая связь между
модулями. Серверная версия сделана правильно — со своим экземпляром.

### `page-locks.ts` (532 строки) — 🟡 мёртвая функция с обманчивым описанием

Логика двух уровней кэша (память воркера 30 с + кэш дата-центра 1 час),
поведение при сбое базы и отличие «миграции нет» от «база упала» (485–500) —
всё сделано верно и соответствует описанию в CLAUDE.md. Белый и чёрный списки
проверил перебором: `/`, `/api`, `/api/...`, `/admin`, `/admin/...` — отсекаются
правильно.

#### 🟡 `isPathHiddenByLock` (528–531) не используется нигде и игнорирует `hideInNav`

```ts
export function isPathHiddenByLock(locks, pathname) {
  const lock = findPageLock(locks, pathname);
  return Boolean(lock);
}
```

Её описание — «Адреса, которые не должны попадать в меню, sitemap и RSS».
Но флаг `hideInNav` (тот самый `~/blog` из `serializeLockPaths`, который
означает «закрыта, но ссылку в меню оставить») здесь не учитывается вообще.

Проверил все вызовы: функция **не вызывается ниоткуда**. Sitemap и RSS
фильтруют напрямую через `findPageLock`, меню — на клиенте в
`src/app/utils/pageLocks.ts`, и там `hideInNav` учитывается правильно.

То есть работающей ошибки нет, но в коде лежит экспортируемая функция,
которая при первом же использовании сломает описанное поведение.

🟡 Мелочь: `sanitizeLockText` (234–241) вырезает `<` и `>`, но не трогает `&`.
Проверю в `page-lock-page.ts`, экранируется ли он при вставке.

### 🟠 `sitemap.xml.ts` — четвёртая копия списка страниц, ничем не связанная с остальными

Строка 8:

```ts
const STATIC_ROUTES = ['/', '/blog', '/cases', '/faq', '/marketing-glossary',
  '/calculator', '/roi-calculator', '/privacy-policy', '/offer', '/cookie-policy',
  '/meta-ads', '/meta-apps', '/google-ads', '/consult'];
```

Такой же список лежит в `scripts/config.js`. Сейчас они совпадают (сверил
поштучно — 14 адресов в обоих, порядок разный). Но:

- CLAUDE.md велит при добавлении индексируемого роута синхронизировать
  `routes.tsx` и `scripts/config.js` — про `sitemap.xml.ts` там **не сказано**;
- теста, связывающего эти два списка, нет. Проверил `generated-seo.test.js`:
  он смотрит только, что в динамическом sitemap нет `/feed.xml` и `/llms.txt`.

Значит новая индексируемая страница будет пре-рендерена, попадёт в роутер —
и **не попадёт в sitemap.xml**. Молча.

Это ровно тот же класс проблемы, что и три копии CSP: одна логика в нескольких
местах, тест сторожит не то.

### `_middleware.ts` (219 строк) — 🟢 норма, одна мелочь

Проверил самое опасное — `ensureApiNotFound` (163–174): она превращает **любой**
HTML-ответ с `/api/*` в JSON-404. Если бы хоть один эндпоинт легитимно отдавал
HTML, он бы сломался молча. Проверил все `functions/api/*` — HTML не отдаёт
ни один (`text/html` встречается только в `article-page.ts`,
`page-lock-page.ts` и самом middleware). Чисто.

Порядок в `onRequest` верный: закрытая страница отвечает заглушкой **до**
вызова `next()`, то есть код настоящей страницы в браузер действительно не
уходит — как и обещано в CLAUDE.md.

`escapeAttribute` (127–129) экранирует `&`, `"`, `<`, `>` — для значения
атрибута в двойных кавычках этого достаточно.

🟡 `isPageRequest` (116–125) считает адрес файлом, если в последнем сегменте
есть точка. Slug статьи с точкой обошёл бы проверку блокировок. Сейчас
недостижимо: `toSafeSlug` точки вырезает.

🟡 `renderSecurityTxt` пересчитывает `Expires` при каждом запросе (строка 25),
поэтому поле фактически никогда не истекает. RFC 9116 ожидает фиксированную
дату. Вреда нет, но смысл поля теряется.

### `page-lock-preview.ts` (213 строк) — 🟠 «обезличенный» хеш обезличивает только на словах

Подпись ссылки предпросмотра сделана правильно: nonce 16 байт, HMAC-SHA256,
сравнение постоянным временем, срок проверяется до подписи, токен обменивается
на HttpOnly-cookie и убирается из адреса (154–166) — причём убирается **и при
неудачной проверке**, так что токен не остаётся в закладках и реферере.
Вывод ключа из пароля админки здесь уместен: смена пароля обязана гасить
выданные ссылки.

#### 🟠 `actorHash` без соли

Строки 209–213:

```ts
export async function actorHash(request: Request): Promise<string> {
  const ip = request.headers.get('CF-Connecting-IP') || 'unknown';
  const agent = request.headers.get('User-Agent') || 'unknown';
  return (await sha256Hex(`page-lock-actor|${ip}|${agent}`)).slice(0, 32);
}
```

Префикс `page-lock-actor|` — открытая константа из исходника. Значит хеш
`IP + User-Agent` подбирается перебором: адресов IPv4 всего 2³², а строк
user-agent — считанные тысячи популярных. Комментарий «сырые IP и user-agent
не хранятся» формально верен, но обезличивания здесь нет.

Показательно, что в этом же проекте так делать умеют: `recordPageStats`
в `leads.ts` солит отпечаток секретом. Здесь соль просто забыли, хотя ключ
подписи лежит в том же файле — `signingKey(env)`.

🟡 Штамп формы (`createFormStamp`/`verifyFormStamp`) не одноразовый: один
подписанный штамп два часа годится для любого числа отправок. От потока
защищает только лимитер (`page_lock_notify`, 10 за 10 минут). Заявленная цель
(«не пачкой и не мгновенно») достигается, но «одноразовость» нигде и не
обещана — отмечаю, чтобы не считать это гарантией.

### `page-lock-page.ts` (652 строки) — 🟡 мёртвый CSS, экранирование в порядке

Закрыл вопрос из `page-locks.ts`: `sanitizeLockText` не трогает `&`, но при
вставке в разметку всё проходит через `escapeHtml` (43–50), который экранирует
`&`, `<`, `>`, `"` и `'`. Проверил каждую точку вставки — заголовок, текст,
дату, путь, штамп формы, подсказки. Дыры нет.

Заглушка отвечает 503 + `noindex` и в мета-теге, и в заголовке — как описано.
Ловушка для ботов (`.trap`) закрыта `aria-hidden` и `tabindex="-1"`.
`min-height: 100vh` с последующим `100dvh` — правильная лесенка для мобильных.

#### 🟡 Примерно 40 строк CSS, который ни к чему не применяется

- `.legal`, `.legal a`, `.legal-note`, `.legal-note a` (строки 379–411) —
  в разметке `renderPageLockHtml` **нет ни одного элемента с этими классами**.
  Остаток прошлой версии. Уходит в браузер при каждом показе заглушки.
- `.sr-only` (418–425) — тоже не используется.
- `.consent a`, `.consent-optional`, `.consent-note` объявлены **трижды**
  (322–329, 371–378, 412–417). Первые два блока полностью перекрываются
  третьим; значения совпадают, поэтому удаление первых двух ничего не меняет
  визуально.
- `.legal` содержит `margin-top: 10px` (381) и следом `margin: 10px 0 0` (384) —
  сокращённая запись перекрывает длинную тем же значением.

Это тот редкий случай правки в CSS, который безопасен по правилу «скриншот до
и после совпадает»: удаляются только объявления, которые ни на что не влияют.

### `site-content.ts` (603 строки) — 🟠 молчаливое удаление и ещё один разъезжающийся список

Санитайзеры разобраны по одному. Приём «`clearableText` против `text`»
(306–319) сделан осознанно и правильно: у части полей пустая строка — это
«очистить», а у части — «не трогать». Пустые массивы `titleLines` и `stats`
в hero сохраняются намеренно, и это прокомментировано.

#### 🟠 FAQ-вопрос с «неправильной» категорией удаляется молча

Строка 557:

```ts
if (!question || !answer || !category || !FAQ_CATEGORIES.has(category)) return null;
```

Категория не из белого списка — и **весь вопрос выбрасывается**. Сервер
отвечает успехом, вопрос из админки исчезает. Ни ошибки, ни предупреждения.

Список категорий при этом лежит в двух местах:

| Где | Что |
|---|---|
| `functions/_lib/site-content.ts:531` | белый список сервера (`Set`) |
| `src/app/pages/FAQPage.tsx:14` | список для админки (`FAQ_CATEGORIES`) |

Сейчас они совпадают — сверил все семь значений. Теста, который бы за этим
следил, нет. Добавите категорию только на клиенте — админка её предложит,
владелец ей воспользуется, а сервер тихо съест вопрос при сохранении.

Третий случай одного и того же класса за аудит: CSP в трёх местах, список
страниц для sitemap в двух, категории FAQ в двух.

#### 🟡 `safeJsonObject` — мёртвый экспорт с ловушкой внутри

Строки 585–592: функция всегда применяет **сервисный** санитайзер, независимо
от того, какой раздел ей дали. Для FAQ или главной это вырезало бы почти всё.
Проверил вызовы — её не зовут нигде, рядом используется правильная
`safeSiteJsonObject` с ключом. То есть сейчас безвредно, но оставлять такую
функцию рядом с правильной опасно: перепутать их — дело одной строки.

#### 🟡 `sanitizeStats` возвращает пустой массив там, где соседи возвращают `undefined`

Строка 452: `return stats;` — без проверки на пустоту, в отличие от
`sanitizeCards` (381) и `sanitizeCaseItems` (441). В вызывающем коде проверка
идёт на истинность, а пустой массив истинен, поэтому в контент запишется
`stats: []`. Возможно, так и задумано (способ очистить блок), но соседние
функции ведут себя иначе — на это легко наступить.

### `page-lock-interest.ts` (279 строк) — 🟢 норма

Повторяет проверенную структуру `lead-quality.ts`: гейт согласия (83), отказ
без единого ключа сопоставления (125), дедупликация по `event_id`, очередь,
честная запись ответа Meta. Отдельное событие `PageInterest` вместо `Lead` —
верное решение, объяснено в шапке.

🟡 Строка 198: `consent_timestamp: Date.now()` — записывается **момент отправки**,
а не момент, когда человек поставил галочку. В `lead-quality.ts` туда идёт
настоящее сохранённое время согласия. На практике разница в секунды (галочку
ставят прямо перед отправкой), но поля называются одинаково, а значат разное.

🟡 В `user_data` нет `country`, хотя `consentRegion` в функцию передаётся.
У обычного `Lead` страна хешируется и уходит. Событие редкое, но это ещё один
случай «данные есть, в Meta не идут».

### `media-folders.ts` (65 строк) — 🟠 папки кириллицей ломают публичную ссылку

`normalizeFolderName` (17–27) **разрешает кириллицу**:

```ts
.replace(/[^a-z0-9а-яё._-]+/gi, '-')
```

А имя файла в `api/admin/upload.ts` приводится к чистому ASCII:

```ts
.replace(/[^a-zA-Z0-9._-]+/g, '-')
```

Итог: ключ в R2 может выглядеть как `uploads/фото/2026-08-26/…-name.webp`,
и публичная ссылка собирается конкатенацией **без кодирования**
([upload.ts:135](functions/api/admin/upload.ts:135)):

```ts
const publicUrl = `${getPublicHost(env)}/${key}`;
```

Браузер такую ссылку обычно вытянет — закодирует сам. Но:

- эта же ссылка уходит в `deleteCacheByUrl` и в Cloudflare purge API, где
  несовпадение кодировки означает, что кэш **не почистится**;
- ссылка сохраняется в HTML статьи как есть и живёт там годами.

Две части одной системы придерживаются разных правил: имя файла обязано быть
латиницей, имя папки — нет. Одно из двух ошибочно.

Разбор `reKeyToFolder` и `folderFromKey` проверил перебором случаев (файл без
папки, файл в папке, старая раскладка по датам) — там всё верно.

### `types.ts` (114 строк) — 🟠 поле-призрак `downloads`

`Article.downloads` (54–57) объявлено в типе и **строго проверяется** при
чтении резервной копии из R2 ([articles.ts:105](functions/_lib/articles.ts:105)).
При этом:

- `normalizeArticles` (jsonbin.ts) его **не переносит** — оно вырезается при
  каждом сохранении;
- `mapRowToArticle` (d1.ts) его **не читает** из базы;
- в интерфейсе его не использует **ничто** — проверил весь `src/`.

То есть поле не может появиться ни в базе, ни в снимке. Но валидатор снимка
готов **забраковать всю резервную копию статей**, если оно вдруг окажется
неправильной формы. Проверка того, чего не бывает, ценой отказа от всего
резерва.

🟡 `JSONBIN_BIN_ID` и `JSONBIN_MASTER_KEY` объявлены обязательными, хотя проект
перешёл на D1 и эти секреты могут быть не заданы. Тип обещает то, чего в бою
может не быть.

### `admin-crm.ts` (292 строки) — 🟠 пять лишних запросов к базе на каждое действие в CRM

`getAdminCrmCapabilities` (147–205) при каждом вызове выполняет **пять**
обращений к базе: четыре `PRAGMA table_info` и `SELECT name FROM sqlite_master`.
Кэша нет.

А зовут её из `assertAdminCrmSchema`, то есть **на каждом запросе** к разделу
«Заявки»: список, доска, изменение этапа, заметки, задачи. Открыли раздел —
несколько эндпоинтов, каждый по пять служебных запросов сверх полезных.

Показательно, что во всех остальных модулях проекта такую же проверку схемы
кэшируют на пять минут — `getLeadsColumns` (leads.ts:47),
`getArticlesColumns` (d1.ts:34), `getDiagnosticsColumns` (meta-diagnostics.ts:137).
Здесь приём просто не применили.

Это прямо относится к вопросу о расходе лимитов Cloudflare: схема меняется
раз в несколько месяцев при накатывании миграции, а спрашивают о ней сотни раз
в день. Лечится тем же кэшем на пять минут, что и у соседей.

🟡 `createCrmActivityStatement` (288): при отсутствии `actionId` подставляется
случайный UUID. Для записи в журнал это нормально, но означает, что защиты от
повторной записи одного действия здесь нет — она есть только у заметок и задач
(уникальные индексы `idx_crm_notes_action_id`, `idx_crm_tasks_action_id`).

### `admin-alerts.ts` (344 строки) — 🟡 три замечания, логика верная

Отдельно проверил склонение (`plural`, 37–44) — оно написано правильно, что
редкость. Прогнал по значениям: 1 «заявка ждёт», 2–4 «заявки ждут», 5–20
«заявок ждут», 11–19 «заявок ждут», 21 снова «заявка ждёт», 101 «заявка ждёт».
Ошибок нет.

Проверил и разбор даты на строке 80 (`.replace(' ', 'T')` + `'Z'`): колонка
объявлена как `created_at TEXT NOT NULL DEFAULT (datetime('now'))`
(миграция 0008), то есть формат с пробелом — преобразование корректно.
Если бы там лежал ISO с `T`, получилось бы `...ZZ` и в тревоге вместо часов
писалось бы `NaN`. Сейчас не стреляет.

🟠 Строка 75: `datetime(created_at) < datetime('now', '-2 hour')` — обёртка
вокруг колонки, индекс `idx_leads_created` не работает, читается вся таблица
заявок. Сейчас в ней 2 строки, поэтому незаметно. Это тот же приём, что я уже
исправил в `meta-center.ts`.

🟠 `tableExists` (46–51) вызывается **пять раз** за один сбор тревог, каждый
раз запросом к `sqlite_master`. Кэша нет — как и в `admin-crm.ts`. Схема
меняется раз в месяцы, а спрашивают о ней при каждом открытии админки.

🟡 `syncAlerts` (235–279) делает по отдельному запросу на каждый повод —
и на обновление, и на закрытие. Их можно собрать в один `batch`, как это
сделано в `leads.ts`. Плюс счётчик `created` считает и новые поводы, и
«ожившие» — цифра в отчёте завышена.

🟡 `notifyAlerts` (307) — `fetch` в Telegram без таймаута. Та же недоработка,
что и в `sendLeadToTelegram`.

### `admin-lead-quality-status.ts` (327 строк) — 🟡 две мелочи

Логика «доказательств доставки» разобрана целиком и сделана правильно: очередь
считается достаточным доказательством, потому что статус `sent` в ней ставится
только после подтверждения `events_received` от Meta — и это прокомментировано.
Ключ у очереди и у диагностики один и тот же (`id` очереди равен `event_id`
события) — проверил по `lead-quality.ts`, совпадает.

🟡 Строка 225: `ORDER BY datetime(created_at) DESC` — та же обёртка вокруг
колонки, что и в трёх других местах. Здесь она вдобавок **бесполезна**: те же
строки заново сортируются в JS (268–271). То есть база выполняет сортировку,
результат которой не используется.

🟡 `safeReason` (78–87) вычищает из текста ошибки токены, почту и телефоны —
это правильно. Но регулярка телефона `(?:\+?\d[\s().-]*){8,}` цепляет **любую**
последовательность из восьми и более цифр. Метка времени вида `1724680000`
превратится в «[телефон скрыт]». В сообщении об ошибке Meta это может съесть
как раз ту часть, ради которой владелец в него и смотрит.

---

## Итог прохода 1 — `functions/_lib` пройден целиком

26 файлов, 8 379 строк. Ядро сервера написано заметно аккуратнее среднего:
транзакции, идемпотентность, поведение при недоступной базе, гейты согласия —
всё это продумано и прокомментировано.

Систематическое, что вылезло по всему проходу:

1. **Обёртка `datetime()`/`date()` вокруг колонки в `WHERE`** — четыре места,
   каждое отключает индекс и читает таблицу целиком. Одно уже исправлено
   (`meta-center.ts`), остальные — `admin-alerts.ts:75`,
   `admin-lead-quality-status.ts:225`, `attribution.ts:552`.
2. **Проверка схемы без кэша** — `admin-crm.ts` (5 запросов на действие),
   `admin-alerts.ts` (5 запросов на сбор тревог). При том что в трёх других
   модулях такой кэш есть.
3. **Одна логика в нескольких копиях без теста, который бы их связывал** —
   CSP (3 места), список страниц для sitemap (2), категории FAQ (2).
4. **`fetch` наружу без таймаута** — оба вызова Telegram.

---

## Проход 2 — `functions/api` и точки входа

### `geo.ts` (25 строк) — 🟢 норма

Список стран, где требуется явное согласие, полный: 27 стран ЕС + Исландия,
Лихтенштейн, Норвегия (ЕЭЗ) + Великобритания и Швейцария — 32 значения,
пересчитал. Ответ отдаётся с `no-store`, что правильно: он зависит от страны
посетителя и кэшировать его нельзя.

### `article.ts` (5 строк) — 🟠 один редирект реализован трижды, и коды разные

| Где | Как |
|---|---|
| `functions/api/article.ts` | `Response.redirect(..., 308)` |
| `public/_redirects:9` | `/api/article /api/articles 302` |
| `src/app/routes.tsx:237` | React-компонент с `window.location.replace` |

**308 и 302 — разные вещи**: первый постоянный и сохраняет метод запроса,
второй временный. Поисковик и клиентская библиотека поведут себя по-разному
в зависимости от того, какая из трёх реализаций отработала.

Сейчас выигрывает функция (для `/api/*` Pages отдаёт запрос ей), то есть
работает 308, а две другие записи — мёртвый груз, который при этом
противоречит рабочей. Четвёртый случай «одна логика в нескольких копиях».

🟡 Мелочь: `PagesFunction` объявлен без `<Env>`, в отличие от всех остальных
эндпоинтов.

### `vitals.ts` (первые 80 строк) — 🟢 контракт с клиентом сходится

Проверил список метрик с двух сторон: сервер принимает `LCP`, `INP`, `CLS`,
`TTFB` (`THRESHOLDS`, 12–17), клиент шлёт ровно эти четыре
(`src/app/utils/webVitals.ts:13`). Расхождения нет — метрика не потеряется.

Верхние границы (`MAX_VALUES`) и проверка источника запроса на месте, тип
устройства определяется на сервере и из тела не берётся — это правильно.

🟡 `detectDevice` (32–38) — **третья копия** одной и той же функции: такая же
была в `api/lead.ts` (я её свёл к общей при правке про Meta) и такая же теперь
в `_lib/meta-capi.ts`. Эту стоит свести туда же.

### `page-lock-notify.ts` (196 строк) — 🟢 норма, проверял придирчиво

Здесь был мой главный подозреваемый, и он не подтвердился. Разбираю, потому
что случай поучительный.

Форма позволяет оставить что-то одно: почту, телефон или телеграм. В `INSERT`
незаполненные поля уходят пустыми строками, а стоит `ON CONFLICT DO NOTHING`.
Обычный уникальный индекс по `(path, email)` считал бы **всех**, кто оставил
только телефон, одним и тем же контактом — второй такой человек молча получил
бы «вы уже в списке» и потерялся.

Проверил миграцию 0035: индексы сделаны **частичными**, каждый следит только
за заполненным полем:

```sql
CREATE UNIQUE INDEX idx_page_lock_subscribers_email
  ON page_lock_subscribers(path, email) WHERE email <> '';
```

Старый непартиальный индекс из 0034 при этом явно удаляется. И до применения
0035 всё тоже верно: без её колонок форма показывает только почту, значит
пустых почт не бывает. Корректно в обоих состояниях схемы — редкий уровень
аккуратности.

Остальное тоже на месте: ловушка для ботов возвращает «успех» (бот не узнает,
что его отсекли), подпись формы проверяется, контакты принимаются только на
действительно закрытой странице, телефон не достраивается выдуманным кодом
страны, в Telegram сообщение уходит без `parse_mode` — то есть разметка в
чужом вводе безопасна.

### `articles.ts` (120 строк) — 🟠 публичный обход кэша (связано с расходом лимитов D1)

Строка 30:

```ts
const bypassCache = url.searchParams.has('_') || url.searchParams.get('cache') === 'no-store';
```

**Любой** запрос с параметром `_` или `cache=no-store` минует кэш и идёт прямо
в базу. Проверки, кто спрашивает, нет — эндпоинт публичный.

Что это значит на практике: запрос вида `/api/articles?_=1`,
`/api/articles?_=2`, `/api/articles?_=3` заставляет базу **каждый раз** читать
все статьи целиком. Кэш при этом не спасает по определению — его же и просят
пропустить.

Оба параметра нужны по делу: `?_=<время>` использует админка, `?cache=no-store` —
сборка (`scripts/config.js`). Но обход рассчитан на своих, а доступен всем.

Разумно: оставить обход только для запроса с паролем администратора
(`X-Admin-Password` тут уже проверяется в соседних эндпоинтах) либо для
запроса со стороны сборки, а публичному трафику отдавать кэш всегда.

🟡 Строка 108: `details: error.message` уходит наружу в теле ответа. Туда
попадают внутренние сообщения вроде «D1 articles are unavailable and no
last-known-good D1 snapshot can be read» или сырая ошибка SQLite. Логировать —
да, отдавать публично — незачем.

### `site-content.ts` (69 строк) — 🟢 норма

Раздельные политики кэша для браузера и для CDN сделаны осознанно и
прокомментированы. Отсутствие таблицы трактуется как «текста нет» и страница
показывает встроенный вариант — это правило проекта, соблюдено.

🟡 Сравнение `If-None-Match` строгое (строка 19): браузер, приславший «слабый»
валидатор `W/"..."`, получит полный ответ вместо 304. Не ошибка, лишний трафик.

### `page-locks.ts` (32 строки) — 🟢 норма

Только GET, параметров не принимает, отдаёт ровно то, что и так видно на
странице. Кэш выдаётся только когда список получен из базы или из кэша; при
деградации — `no-store`, чтобы не законсервировать неполный список. Верно.

### `meta-outbox-process.ts` (43) и `alerts-run.ts` (56) — 🟡 сравнение секрета не постоянным временем

Обе точки для внешнего cron сравнивают секрет обычным `!==`:

```ts
if (!debugSecret || providedSecret !== debugSecret) { ... }
```

Обычное сравнение строк прерывается на первом несовпавшем символе, то есть
время ответа зависит от того, сколько символов угадано. В этом же проекте для
пароля админки и для подписей написан `safeEqual` с постоянным временем
(`_lib/auth.ts`, `_lib/admin-session.ts`, `_lib/tracking-signature.ts`,
`_lib/page-lock-preview.ts` — четыре реализации), а здесь его не применили.

Опасность невелика: сетевой шум перекрывает разницу, плюс стоит лимитер.
Но приём в проекте есть, и две точки из него выпали.

🟡 `alerts-run.ts:52` отдаёт текст внутренней ошибки — здесь допустимо,
вызывающая сторона уже прошла проверку секретом.
