[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# Все запросы сначала идут на функцию og.js
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/og?path=:splat"
  status = 200
  force = true
